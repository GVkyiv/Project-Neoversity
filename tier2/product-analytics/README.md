# Продуктова аналітика (Product Analytics)

Tier 2. Фінальний результат: A/B-тест — план, статистика (Bootstrapping,
Монте-Карло), інсайти.

У вихідній таблиці знайдено лише «Product Analytics and Applied Statistics» —
ймовірно, версія для іншої спеціалізації (Data Science). Не підтверджено, що
це той самий курс — перевірити перед використанням змісту.
